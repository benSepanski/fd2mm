Run things from `comparision.py`


and can specify

* wave number (omega_list, line 27)
* degree of function space (degree_list, line 29)
* fmm orders (fmm_orders, line 30), accuracy will be about 0.5**fmm_order in 2-d
* method (method_list, line 31)

    You can set methods from 
    ['coupling', 'pml(bdyint)', 'pml(quadratic)', 'transmission',
    'nitsche', 'integral_eq_direct', 'green_to_dirichlet']

This will run computations over all the meshes in the msh_files directory.

Use `make_meshes.py` to make new meshes by tuning `num_refine`
